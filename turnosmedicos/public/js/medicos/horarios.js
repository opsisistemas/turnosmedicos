$(document).on("click", ".btn-show-horario", function () {
	var medico_id = $(this).data('id');
	//pedir al servior, por un lado, todos los días con sus nombres,
	//por otro lado, los días que atiende el médico con sus datos pivot
	$.ajax();
});